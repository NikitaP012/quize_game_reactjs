# Used only for production builds (npm run build) — Vite gives this file
# priority over .env. Points the app at the deployed Node backend on Render.
VITE_API_BASE=https://quize-game-8gmg.onrender.com
